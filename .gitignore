node_modules/
.env
dist/
uploads/*
!uploads/.gitkeep
.DS_Store
logs/
.claude/

# Local database dumps — may contain real user data. NEVER commit.
# (Anchored to repo root so it won't touch server/db/migrations/*.sql.)
/holly.sql
/*.dump
/*.sql.gz

# Prompt-validation fixtures: real client documents are PRIVATE — never commit.
# (Keep the folder + its README; ignore everything else inside.)
server/fixtures/real/*
!server/fixtures/real/README.md
