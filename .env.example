DATABASE_URL=postgresql://localhost:5432/tracker
JWT_SECRET=change-me-to-a-random-string
SERVER_PORT=3001
ADMIN_EMAIL=admin@developai.co
ADMIN_PASSWORD=tracker2025

# Model API keys for the prompt-library validation step (npm run validate-prompts).
# All OPTIONAL: a model with no key is marked 'untested' and the run still succeeds.
ANTHROPIC_API_KEY=        # validates the 'claude' model
OPENAI_API_KEY=           # validates the 'gpt' model
GOOGLE_API_KEY=           # validates the 'gemini' model (Gemini)
# Optional promptfoo model-id overrides (defaults are sensible):
# PF_ANTHROPIC_MODEL=anthropic:messages:claude-sonnet-4-6
# PF_OPENAI_MODEL=openai:gpt-4o-mini
# PF_GEMINI_MODEL=google:gemini-1.5-flash
